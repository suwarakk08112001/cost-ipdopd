import { OnApplicationShutdown, OnModuleInit } from "@nestjs/common";
import { NodeRuntimeMetrics } from "../interfaces/node-runtime-metrics.interface.js";
import { ObserveModuleOptionsWithDefaults } from "../interfaces/observe-options.interface.js";
export declare class NodeRuntimeMetricsService implements OnModuleInit, OnApplicationShutdown {
    private readonly options;
    private readonly logger;
    private eventLoopDelayMonitor;
    private gcObserver;
    private gcCount;
    private gcTotalDuration;
    private gcBreakdown;
    private lastCpuUsage;
    private lastCpuUsageTimestamp;
    private osTotalMemory;
    constructor(options: ObserveModuleOptionsWithDefaults);
    onModuleInit(): void;
    onApplicationShutdown(): Promise<void>;
    monitorEventLoopDelay(): void;
    observeGcPerformance(): void;
    collectNodeRuntimeMetrics(): NodeRuntimeMetrics;
    private resetGcMetrics;
    private updateLastCpuUsage;
}
