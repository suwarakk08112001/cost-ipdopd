import { CompleteTraceEventNode } from "../interfaces/trace-events.interfaces.js";
export declare const TRACE_KEY_MAP: {
    readonly name: "n";
    readonly origin: "o";
    readonly tags: "t";
    readonly duration: "d";
    readonly error: "e";
    readonly className: "c";
    readonly methodKey: "m";
    readonly children: "ch";
    readonly spanId: "s";
    readonly startOffset: "so";
};
export type RecursiveEncodedTrace = Omit<{
    [K in keyof Omit<CompleteTraceEventNode, "children"> as K extends keyof typeof TRACE_KEY_MAP ? (typeof TRACE_KEY_MAP)[K] : never]: CompleteTraceEventNode[K];
}, "ch"> & {
    ch?: Array<RecursiveEncodedTrace>;
};
/**
 * Encodes one span and everything below it.
 *
 * Shared by the request and job encoders rather than duplicated in each: both
 * kinds of operation produce the same span shape, so a reader sees one format
 * whichever produced it, and `encoders.spec.ts` asserts exactly that.
 */
export declare function encodeTrace(trace: CompleteTraceEventNode): RecursiveEncodedTrace;
